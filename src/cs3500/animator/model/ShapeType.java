package cs3500.animator.model;

/**
 * Type of various kinds of 2D shapes in animation, although
 * currently we have described only rectangles and ovals
 * Rectangle : it represent the name of shape for rectangle, which
 * will have one position, width, height, color, and appears
 * Oval : it represents the name of shape for oval, which
 * will have one position, X adn Y radius, color ,appears and disappears.
 */
public enum ShapeType {
  RECTANGLE, OVAL;
}
